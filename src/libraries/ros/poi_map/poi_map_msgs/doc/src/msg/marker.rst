Marker
======

`Marker` is an auxiliary ROS message type describing a PoI (Point of Interest) marker (i.e. a 2D point with a matching score value)

.. literalinclude:: ../../../msg/Marker.msg
   :language: bash
   :lines: 14-
